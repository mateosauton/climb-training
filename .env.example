# Public browser configuration only. Copy to .env.local and set values in
# deployment settings. Do not place service-role keys, SMTP credentials, or
# Edge Function provider keys in a VITE_ variable.
VITE_SUPABASE_URL=
VITE_SUPABASE_PUBLISHABLE_KEY=
